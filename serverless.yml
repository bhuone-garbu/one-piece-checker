service: one-piece-chapter-checker

frameworkVersion: '2'

plugins:
  - serverless-dotenv-plugin
  - serverless-bundle
  - serverless-offline-scheduler

provider:
  name: aws
  region: us-east-1
  runtime: nodejs14.x
  lambdaHashingVersion: 20201221

functions:
  checkManga:
    handler: src/handlers/check-manga.handler
    events:
      # Invoke Lambda function every minute
      - schedule:
          rate: rate(1 minute)
          enabled: true

custom:
  bundle:
    sourcemaps: false
